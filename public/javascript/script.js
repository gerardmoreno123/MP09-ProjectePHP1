// script.js
document.addEventListener('DOMContentLoaded', () => {
    // Activar el modo oscuro al cargar la página
    document.body.classList.add('dark-mode');
});

// Puedes eliminar el resto del código relacionado con el cambio de tema
